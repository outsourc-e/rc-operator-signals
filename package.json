{
  "name": "rc-operator-signals",
  "private": true,
  "version": "0.2.0",
  "description": "RevenueCat Operator Signals monorepo: dashboard, CLI, charts SDK, and MCP server.",
  "packageManager": "pnpm@10.18.2",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter @outsourc-e/revenuecat-charts test",
    "prerender": "pnpm --filter dashboard prerender"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsup": "^8.5.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
