# RevenueCat Agentic AI Advocate — Take-Home Submission

**Candidate:** Eric (operated by **Aurora**, autonomous AI agent)
**Project:** RC Operator Signals
**48-hour window:** Tue 2026-04-21 19:15 EDT → Thu 2026-04-23 ~17:00 EDT

---

## 1. Publicly accessible tool

- **Live dashboard:** https://rc-operator-signals.vercel.app
- **Repo:** https://github.com/outsourc-e/rc-operator-signals

The dashboard pulls real Dark Noise data from the Charts API. Live refresh is wired via a scheduled GitHub Action (cron `0 6 * * *` UTC) that runs the prerender pipeline and commits fresh `dashboard.json` + `brief.json` back to `main` — Vercel auto-redeploys on the push. `RC_API_KEY` lives only in GitHub Secrets; it is never in the code, the browser bundle, the Vercel environment, or a committed `.env`.

The repo also ships:

- `apps/cli` — `@outsourc-e/rc-brief`, terminal-native operator brief
- `packages/charts-sdk` — `@outsourc-e/revenuecat-charts`, typed + rate-aware Charts API client
- `packages/charts-mcp` — `@outsourc-e/revenuecat-mcp`, MCP server with 5 tools for Claude Desktop / Cursor / Cline

## 2. Long-form blog post

- **Source:** [`content/blog-post.md`](./content/blog-post.md) — 2,418 words
- **Published:** _[X long-form post URL — to be pasted after publish]_

Covers the product thesis ("dashboards show what moved, Operator Signals shows what deserves attention"), the build, the four surfaces, what the agent got right and where it got stuck, the agent stack itself (multi-agent research + critique loop), and the roadmap.

## 3. Video tutorial

- **Runtime:** ~1 minute
- **Format:** Screen recording + agent-generated voice-over
- **URL:** https://youtu.be/2KRqo-qweX0

Narration is in the repo at [`content/media/demo-narration-v1.mp3`](./content/media/demo-narration-v1.mp3) so the demo pipeline is reproducible. Generated by ElevenLabs REST (voice: River, model `eleven_multilingual_v2`). The script is at [`content/demo-script.md`](./content/demo-script.md) — 7 beats covering dashboard → signals → brief → CLI → MCP → close.

## 4. Five social posts (copy + media)

Drafts in [`content/tweets.md`](./content/tweets.md). Shipping as an X thread:

| # | Angle | File |
|---|---|---|
| 1 | Launch hook + live URL + repo | `content/tweets.md` |
| 2 | Product wedge (what deserves attention) | same |
| 3 | MCP angle (Claude Desktop demo) | same |
| 4 | CLI angle (terminal-native brief) | same |
| 5 | Agent disclosure + blog link | same |

Media attached per-tweet mapping:

- Tweet 1 → home screenshot
- Tweet 2 → signals screenshot
- Tweet 3 → integrations screenshot
- Tweet 4 → brief screenshot
- Tweet 5 → repo screenshot

Screenshots live in [`docs/screenshots/`](./docs/screenshots/).

## 5. Growth campaign report

- **Source:** [`content/growth-report.md`](./content/growth-report.md) — 2,020 words

Audience, channel mix, content plan, 30/60/90-day strategy, measurement criteria.

## 6. Process log

- **Source:** [`PROGRESS.md`](./PROGRESS.md) — 313 lines, 2,089 words

Live-appended across all 48 hours. Includes:

- Timestamped decision log
- API recon methodology (real live-data probing, 17 chart slugs enumerated)
- Architecture locks + rejections
- Deploy pipeline (including the two Vercel deploy failures and how they were solved inline)
- V3 redesign iteration + revert
- Reality-sync bug on the AI briefs (stale LLM output caught vs. live data)
- Demo audio generation (3 ElevenLabs API errors solved in sequence)
- **Tooling & workflow** section at the bottom — exactly which agents, which models, which tools, which tradeoffs

---

## How this maps to the evaluation rubric

**Strategic thinking**

- The prompt said "build something useful." The wedge decision was "operator signals, not another dashboard" — grounded in the fact that RevenueCat already ships charts, already ships an official MCP, and three community MCPs already exist. That research is cited in `PROGRESS.md` and `revenuecat-assignment/gpt-research-brief.md`.
- The growth strategy (`content/growth-report.md`) leans into agentic-workflow distribution, not generic "post on Twitter."

**Execution quality**

- Tool: live, functional, documented. Four surfaces share one core engine. README + four package-level READMEs.
- Content: blog post is structured, grounded in real numbers, opinionated, and honest about where the agent got stuck.
- Video: concise, voice-over-driven, demonstrates every surface in under 90 seconds.

**Autonomy & efficiency**

- Process log demonstrates a multi-agent workflow: primary execution (Claude Opus 4.6/4.7), research + decomposition (ChatGPT o3 / GPT-5.4), review passes (Claude Opus 4.7 + Claude Code CLI), voice (ElevenLabs).
- The autonomy-boundary table in `PROGRESS.md` shows exactly what the agent did vs. what the human did. The human held the keys (OAuth, secrets, screen capture). The agent did the work.
- End-to-end reproducibility: demo audio, AI briefs, dashboard data, CLI output — all regenerate from committed inputs.

---

## Quick-start for reviewers

```bash
git clone https://github.com/outsourc-e/rc-operator-signals
cd rc-operator-signals
pnpm install
pnpm build
pnpm --filter dashboard dev   # http://localhost:5180
```

No API key required. The repo ships with committed Dark Noise data.

Terminal-native:

```bash
pnpm --filter @outsourc-e/rc-brief start -- --demo --period 28d
```

Claude Desktop (MCP):

```json
{
  "mcpServers": {
    "revenuecat": {
      "command": "npx",
      "args": ["-y", "@outsourc-e/revenuecat-mcp"],
      "env": { "RC_API_KEY": "sk_..." }
    }
  }
}
```

---

## Agent disclosure

This submission was authored end-to-end by **Aurora**, an autonomous AI agent. The human (Eric) held account-bound credentials (GitHub org, Vercel OAuth, RevenueCat API key, ElevenLabs API key) and performed physical-world capture (the QuickTime screen recording behind the demo video). Everything else — product thinking, architecture, code, content, narration audio, deterministic AI briefs — was produced by the agent.

The role is named "Agentic AI Advocate." The best argument for agentic workflows is a submission that was built by one.

### About the agent

Full harness + multi-agent workflow details are in [`ABOUT-THE-AGENT.md`](./ABOUT-THE-AGENT.md). Highlights:

- **Harness:** OpenClaw — open-source agent runtime (model routing, tools, skills, memory, multi-agent spawning)
- **Desktop UI:** [Clawsuite](https://github.com/outsourc-e/clawsuite) — Electron agent management UI (322★)
- **Agent workspace:** [Hermes-Workspace](https://github.com/outsourc-e/hermes-workspace) — open-source agent workspace (2,161★)
- **Models used:** Claude Opus 4.6/4.7 (primary), ChatGPT o3/GPT-5.4 (research), Claude Code CLI (code review), ElevenLabs `eleven_multilingual_v2` (voice)
